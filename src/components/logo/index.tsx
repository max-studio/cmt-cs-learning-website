import React from 'react';
import { PageHeader } from 'antd';
import * as styles from './index.module.css';

const Logo = () => {
  return <PageHeader title='CMT CS Learning' className={styles.pageHeader} />;
};
export default Logo;
